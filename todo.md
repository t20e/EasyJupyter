# TODO

- [ ] A better search in notebooks is a must-have, VSC currently struggles with searches in notebooks. Same for the TODO extension.
- [ ] Include the How to create LLM project as an example.
- [ ] Any other warnings I can give the user, if they wrongly use EasyJupyter in a notebook?
- [ ] How should errors be handled? For example, if user gets an error because they didn't use torch correctly, how should I handle that? Maybe they should always test in a notebook first?
- [ ] `easyjupyter -version` doesn't work, how should I handle this?
- [ ] I am importing the library as `import EasyJupyter` in files, its capitalized, should I do something about this? However, all the other commands are lowercase.
